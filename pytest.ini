[pytest]
pythonpath = src tests
testpaths = tests
# Sem isto, o pytest coletaria as funcoes testar_* importadas dos modulos
# como se fossem casos de teste.
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning:dateutil.*
